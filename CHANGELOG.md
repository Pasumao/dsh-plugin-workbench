# Changelog

## [0.0.13] - 2026-08-26

### Fixed

- **预览分栏宽度拖拽：一拖就骤然缩小到最窄，之后无法拖动、无法复原**。根因：
  拖拽计算"预览最大宽度"时用 `handle.parentElement` 测量中间列宽度，但 slot 系统把
  slot 内容包在一层 `display: contents` 的包装元素里——该包装层不参与布局，
  `getBoundingClientRect().width` 恒为 **0**，于是
  `max = max(240, 0 - 240) = 240`，第一次 `pointermove` 就把宽度钳到最小宽度 240，
  之后每次移动都被钳在 240，表现为骤然缩小且拖不回来（100% 复现，与窗口/鼠标操作
  无关）。修复：`laidOutParent()` 跳过所有 `display: contents` 层，取真正参与布局的
  flex 容器（中间列）再测量；持久化宽度恢复时同样用它校准。
- 拖拽健壮性（防同类"卡死"）：`setPointerCapture` 保证 `pointerup` 在指针移出窗口后
  也能派发并清理监听器；监听 `pointercancel`；每次 `pointerdown` 防御性移除上一次
  残留的监听器；组件卸载时清理窗口监听器；拖拽中每步实时重测中间列宽度。

### Added

- **预览宽度持久化**：拖拽后的分栏宽度写入 localStorage，刷新/重开会话后恢复
  （自动按当前窗宽 clamp 在有效区间内）。


## [0.0.12] - 2026-08-25

- 相关插件段新增 dsh-plugin-windows-guard（Windows 环境防坑守则 skill 插件，互相引流）。


本项目所有重要变更都会记录在此文件。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [0.0.11] - 2026-08-21

### Added

- **README 互相引用**：新增「相关插件」段，列出同系列已发布插件（npm / GitHub 链接 + 一句话说明），互相引流。

## [0.0.10] - 2026-08-21

### Fixed

- **「在资源管理器打开」不生效（窗口打开但不可见）**：Windows 下 `spawn` 传
  `windowsHide: true` 会让新开的资源管理器文件夹窗口以**隐藏状态**创建
  （CREATE_NO_WINDOW 沿请求一路传导，窗口存在但 `visible=False`，用户什么都
  看不到）。`explorer.exe` 是 GUI 子系统程序，不带 `windowsHide` 也不会闪
  控制台；`reveal` 端点现在对 explorer.exe 全部不传该标志（含 WSL 转译分支），
  资源管理器窗口正常弹出并选中目标文件

### Added

- **整列右键菜单**：不只文件行——在左栏任何空白处、头部、剪贴板条上右键
  都弹出当前工作区目录菜单（新建文件 / 新建文件夹 / 粘贴 / 全选 / 撤销 /
  复制路径 / 在资源管理器打开 / 在系统中打开 / 刷新）；右键菜单自身不再触发
  浏览器原生菜单
- **@ 在消息中引用（Claude Code 式文件引用）**：文件右键菜单新增
  「@ 在消息中引用」，把 `@相对工作区路径`（自动带尾随空格）插入聊天输入框
  光标处；发送后消息里的 `@相对路径` 渲染为超链接（点击在工作台预览中打开
  该文件），语法为 `@` + 相对工作区路径——`@src/a.ts`、`@README.md` 匹配，
  绝对路径 / 邮箱 / 无路径形状的普通 `@文本` 一律按原样显示、无特殊含义；
  中文句子标点跟尾视为普通文本
- **拖文件进会话**：把左栏文件（含多选）拖到聊天区任意位置（不在左栏内）
  即把路径文本插入聊天输入框光标处；拖到左栏目录行仍是原有的移动操作
- **任意位置点击取消选中**：选中文件/文件夹后，点击树外任何地方（会话、
  其他栏、左栏头部）都会清除选中；树内空白/提示行点击同样取消

## [0.0.9] - 2026-08-21

### Added

- **布局补丁自动修复（自愈）**：插件 host 端在启动时自动检测
  `dsh-client-ui-layout` 客户端 bundle 是否还带 explorer 列补丁；若已被
  DSH 升级 / `pnpm install` 覆盖还原（这正是 workbench 列无声消失的根因），
  会自动重跑 `scripts/patch-layout.mjs` 重打。幂等（已打则跳过）、非阻塞
  （spawn 后台执行，不影响启动）、安全（锚点不匹配时脚本 abort 不写文件，
  仅记录警告，插件照常启动）。

## [0.0.8] - 2026-08-20

### Fixed

- **删除/复制被占用文件夹不再报裸 "permission denied"**：Windows 上文件夹或其
  内部任一文件被其他程序占用（编辑器、资源管理器、终端 cd 其中、杀毒扫描）时，
  改名/删除会以 EPERM/EBUSY 失败。host 端 `rename` / `delete` / `copy` 端点
  现在先做**有界重试**（3 次、150/300ms 间隔），可自动扛过索引器/杀毒的瞬时
  占用；仍失败时返回可操作的提示「`名称` is in use by another program —
  close any editor/Explorer view of it (or a terminal inside it) and retry」，
  替代原先无指引的 "permission denied"
- **树内复制文本不再误触发文件复制**：在树中选中文件/文件夹后，再去选中树内
  的实际文本（如行内错误提示）按 Ctrl/Cmd+C/X 时，此前会拦截浏览器原生复制、
  把选中的文件放进插件剪贴板。现改为：存在非空文本选区或焦点在可编辑元素时，
  快捷键交给浏览器原生处理，复制的是文字而非文件

## [0.0.7] - 2026-08-19

### Fixed

- **跨工作区粘贴（复制冲突）不再报 Zod 校验错误**：复制/粘贴遇到目标已存在时，
  host 端此前返回自定义错误码 `code: 'exists'`，而通用 RPC 通道的错误体受核心
  封闭错误码联合校验（`rpcErrorSchema`）约束，客户端解析直接抛 `invalid_union`，
  覆盖确认对话框根本不会出现。现改为在**成功值**中返回 `exists: true` 标志
  （OS 文件管理器语义），客户端据此询问是否覆盖，行为不变、报错消除

## [0.0.6] - 2026-08-19

### Added

- **在资源管理器打开**：右键菜单新增「在资源管理器打开」
  （`menu.revealInExplorer`，同 VS Code 的 Reveal in File Explorer），经
  loopback RPC 的 `reveal` 端点把文件/文件夹定位到系统文件管理器——
  文件在所在文件夹中被选中（Windows `explorer /select,`、macOS `open -R`），
  文件夹直接打开（`explorer <dir>` / `open <dir>`）；WSL 先经 `wslpath -w`
  转译再交给 Windows 桌面，桌面 Linux 用 `xdg-open`（文件 → 父目录）；
  路径仍先经 `ctx.fs.resolve → stat`（沙箱一致的路径解析）再执行
- **Markdown 渲染预览**：打开 `.md` 文件默认渲染（markdown-it，`html: false`
  转义原始 HTML，代码块复用已注册的 highlight.js 语言高亮），标签栏按钮
  一键切换回可编辑源码视图，再次点击切回渲染；相对路径图片自动重写到同源
  字节路由 `/dsh-plugin-files/raw/<path>`，README 插图可直接显示；
  超过 256KB 的 md 默认进源码视图并提示
- **打开文件磁盘变更检测**：宿主按标签集合对每个文件所在目录做 `fs.watch`
  （父目录监听可存活编辑器原子重命名），事件按路径去抖后经同源 SSE 路由
  `/dsh-plugin-files/events` 推送；干净的标签页自动重读同步，带未保存编辑的
  标签页保留草稿并在标签上显示「⟳」徽标（点击重新加载、放弃未保存编辑）；
  插件自身保存会在短窗口内抑制回波，避免保存被当成外部变更
- **批量删除**：多选（Ctrl/⌘+点击、Ctrl+A）后，右键菜单显示「删除选中的
  N 项」，Delete 键同样生效；先删子项再删父项，已删除文件夹下的项自动跳过
- **拖拽移动**：把文件/文件夹（含整个多选）拖到任意目录行上松手即移动；
  拖到树空白区域 = 移入当前目录；目标目录高亮描边，文件夹拖进自身或自己
  子目录自动忽略，拖到所在目录本身为无操作
- **操作可撤销**：右上角「↩」按钮或 Ctrl+Z 撤销最近的操作（每个工作区
  独立、上限 30 条），覆盖：复制（撤销删除副本）、剪切/拖拽移动（撤销移回
  原位并重定向已打开标签）、重命名（撤销改名）、新建（撤销移除）、删除。
  删除改为「可撤销删除」：把项目瞬移（重命名）进其旁的隐藏 `.dsh-trash`
  文件夹（不复制字节），撤销即移回原位；撤销栈溢出时自动清空被挤出条目的
  回收站内容，回收站文件夹在树中隐藏显示

## [0.0.5] - 2026-08-17

### Added

- **图片预览**：打开 png/jpg/jpeg/gif/webp/avif/bmp/ico/svg 直接在预览栏渲染，
  不再显示「二进制文件」提示。字节经同源路由 `/dsh-plugin-files/raw/<path>`
  提供：宿主先经 `ctx.fs.resolve → stat`（沙箱一致的路径解析）再读字节，
  仅限图片扩展名、20MB 上限；加载失败显示内联提示，切换标签自动复位
- **编辑器行号栏**：左侧逻辑行号（VS Code 风格，软换行下仍按源行编号），
  与编辑区滚动逐帧锁定对齐；纯文本模式（md/txt/大文件）同样生效
- **右键菜单（VS Code 风格）**：
  - 文件：打开预览 / 复制路径 / 在系统中打开 / 重命名 / 删除
  - 文件夹：新建文件 / 新建文件夹 / 复制路径 / 在系统中打开 / 刷新 /
    重命名 / 删除（递归删除带确认）
  - 空白区域：新建文件 / 新建文件夹 / 复制路径 / 刷新
  - 菜单自动夹紧视口；点击外部 / Esc / 窗口失焦自动关闭；操作失败在
    对应目录行内联红色提示；重命名/删除自动维护已打开的标签页

### Changed

- 宿主 RPC 通道新增 `createFile` / `createDir` / `rename` / `delete` 四个
  端点（node:fs 实现，经 `ctx.fs.resolve` 统一解析；rename 拒绝覆盖已存在
  目标，delete 按 stat 决定递归）；`rpc.handle` 注册改为 effect 包裹，
  HMR/停用循环不再泄漏路由

### Fixed

- **右键菜单文字看不清**：maid-atelier 皮肤对 `[role='menu']` 有全局规则，
  会把菜单文字色劫持为「页面主题」色，而菜单背景跟随「workbench 主题」，
  两者不一致时文字与背景同色系。菜单颜色现已锁定到 workbench 主题变量
  （`!important`），任何皮肤组合下文字都与背景保持对比

## [0.0.4] - 2026-08-17

### Fixed

- 长中文文本（如小说 .txt）预览卡顿：无语法高亮的文件不再渲染高亮叠加层，
  直接由 textarea 显示文本；代码文件超过 64KB 自动降级为纯文本编辑，
  避免每次按键重新注入并布局几十万字符的两层文本
- **md 文件一律以纯文本模式显示**（不再做语法高亮叠加）：20KB+ 的 md 加载
  从约 60ms 降到约 10ms，彻底消除 md 的拖选错位与加载卡顿；大文件加载时
  先让浏览器绘制一帧再渲染内容，界面不再在打开文件的同一帧内冻结
- md 长文本：去掉悬停时反复弹出的「Ctrl+S 保存」提示；高亮层加粗改为
  纯颜色强调（粗体改变字宽会导致拖选时高亮层与文本换行错位），并增加
  每帧滚动锁定，保证拖选自动滚动时两层始终对齐
- **拖选错位根因**：textarea 底部会预留约一行给光标，导致其可滚动范围比
  高亮层大，长文件滚到接近底部时高亮层文本比选中框低约一行（选中框浮在
  显示文本上方）。已给高亮层补齐等量底部滚动余量，两层滚动范围完全一致
- 打开/切换含大量文件的目录不再卡顿：文件树行改为 memo 组件，点击文件
  只重渲染受影响的单行（此前每次点击都重渲染整棵文件树）；切换工作区时
  恢复已展开目录改为分批加载

## [0.0.3] - 2026-08-16

### Added

- 文件图标：常见格式按扩展名显示着色徽章（TS/JS/PY/HTML/CSS 等代码文件）、
  emoji（图片、音视频、压缩包、Office 文档、安装包等），目录展开/收起图标区分
- 自动换行开关：标签栏一键在「软换行（仅显示层换行，不改动文件）」与
  「长行横向滚动」之间切换；偏好写入 localStorage 持久化
- 高亮层与编辑层滚动槽对齐、tab 缩进对齐修复

## [0.0.1] - 2026-08-14

### Added

- 初始版本发布（https://github.com/Pasumao/dsh-plugin-workbench）：
  - VS Code 风格文件树列：跟随会话工作区、懒加载展开、每 2 秒自动刷新
  - 可编辑分栏预览：语法高亮层 + 透明 textarea、`Ctrl+S`/`Cmd+S` 保存、
    脏标记圆点、保存失败提示
  - 标签页：多文件同时打开、点选/拖拽排序/逐个关闭、收起/弹出
  - 明暗主题：VS Code 亮色/暗色配色一键切换（独立于 app 皮肤）
  - 按工作区隔离：每个 cwd 各自保存标签、激活文件、收起与展开状态
  - 内置 highlight.js：JS/TS、Python、JSON、HTML/XML、CSS、Markdown、Shell、
    Java、C/C++、C#、Go、Rust、SQL、YAML、INI
  - 布局补丁脚本 `scripts/patch-layout.mjs`：4 列布局 + explorer 槽位 +
    中心分栏（幂等、可回滚、锚点校验）
  - MIT 许可
